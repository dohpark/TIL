const solution = (bridgeLength, weight, truckWeigths) => {
  let answer = 1;
  let passingWeight = 0;
  let passingTrucks = [];
  let i = 0;
  // 핵심
  while (truckWeights.length > 0) {
    if (passingWeight < limit) {
    }
  }

  return answer;
};
